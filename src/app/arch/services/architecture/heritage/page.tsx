import HeritageServicesClient from './client';

export const metadata = {
    title: 'Heritage & Conservation Architects | Architectural Design',
    description: 'Specialist heritage architects dedicated to the sensitive restoration and adaptive reuse of historic buildings.',
};

export default function Page() {
    return <HeritageServicesClient />;
}
